let { compile } = require('./dist/src')
let { readFileSync } = require('fs')

compile({
  "title": "Cycle (4)",
  "type": "object",
  "properties": {
    "foo": {
      "type": "integer"
    },
    "bar": {
      "$ref": "cycle.3.json"
    }
  }
}, 'foo', { cwd: 'test/resources2/' })